const { SlashCommandBuilder, EmbedBuilder } = require("discord.js");

module.exports = {
  data: new SlashCommandBuilder()
    .setName('name') // Replace the name of your command to your command name.
    .setDescription('description') // Replace the description of your command to your command description.
    
    async execute(interaction) {
    
    
    }
 };
